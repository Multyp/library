class Book {
  constructor(id, title, author, description, pages, isRead) {
    this.id = id;
    this.title = title;
    this.author = author;
    this.description = description;
    this.pages = pages;
    this.isRead = isRead;
  }
}

export default Book;
